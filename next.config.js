// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    serverActions: {}, // Change this from true/false to an empty object
  },
  // Other configurations...
};

module.exports = nextConfig;